# Browser-visible Vite variables must start with VITE_.
# Never place private API keys here. Future server secrets belong in Cloudflare
# Workers and should be added with `wrangler secret put`.
VITE_APP_URL="http://localhost:3000"
